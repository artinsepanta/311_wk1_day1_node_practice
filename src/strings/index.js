const split = (str, delim) => {
  const delim=str.split(" ")
  console.log(delim[3])
}

const pairs = (str) => {
 

}

const reverse = (str) => {
  // write code for strings.reverse

}

module.exports = {
  split,
  pairs,
  reverse
}